{
  "name": "bitcorp-erp",
  "version": "1.0.0",
  "description": "Bitcorp ERP System",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint",
    "test": "cd frontend && npm run test",
    "prepare": "[ -x ./node_modules/.bin/husky ] && ./node_modules/.bin/husky install || echo 'husky not installed, skipping prepare'"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "husky": "^9.1.7",
    "next": "^15.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
