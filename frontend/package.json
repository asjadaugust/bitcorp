{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx next dev",
    "build": "NODE_OPTIONS=\"--max-old-space-size=8192\" npx next build",
    "start": "npx next start",
    "lint": "npx next lint",
    "type-check": "npx tsc -p . --noEmit",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0"
  }
}
